class Node:
   def __init__(self,data):
      self.data=data
      self.next=None

class Linkedlist:
   def __init__(self):
      self.head=None

   def print_list(self):
      curr=my_list.head
      while curr is not None:
         print(curr.data, end="->")
         curr=curr.next
      print("None")

my_list=Linkedlist()
n=int(input("Enter no of elements:"))
val=[1]*n
print("Enter elements:")
for i in range(n):
   v=int(input())
   val[i]=Node(v)
my_list.head=val[0]
for i in range(0,n-1):
   val[i].next=val[i+1]
val[i].next=my_list.head
my_list.print_list()

# Find cycle exists or not

slow=my_list.head
fast=my_list.head
flag=False
while fast is not None:
    slow=slow.next
    fast=fast.next.next
    if slow==fast:
        flag=True
        break
    
if(flag):
    print("There is cycle in list ")
else:
    print("There is no cycle in list ")


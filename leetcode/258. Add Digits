class Solution(object):
    def addDigits(self, num):
        """
        :type num: int
        :rtype: int
        """
        """rem=0
        while num>10:
            s=0
            while num!=0:
                rem=num%10
                s+=rem
                num=num//10
            num=s"""
        if num==0: 
            return 0
        return 1+(num-1)%9
